import {describe, expect, it} from 'vitest';

// TODO(seam): This needs a routed integration harness for the query-backed book browser,
// PrimeNG overlays, and the persisted filter/sidebar preference services before the
// control-flow branches can be exercised without brittle template scaffolding.
describe.skip('BookBrowserComponent', () => {
  it('needs a browser-level harness for routed query state, filters, and overlay actions', () => {
    expect.hasAssertions();
  });
});
