import {describe, expect, it} from 'vitest';

// TODO(seam): The ebook footer is driven by reader state signals, progress controls, and
// layout actions that need a mounted reader harness instead of a shallow class test.
describe.skip('EbookFooterComponent', () => {
  it('needs a mounted reader harness for progress, navigation, and selection footer branches', () => {
    expect.hasAssertions();
  });
});
