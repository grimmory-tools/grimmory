import {describe, expect, it} from 'vitest';

// TODO(seam): OPDS settings coordinate clipboard access, confirmation dialogs, PrimeNG
// tables, and remote user CRUD, so they should be exercised with a mounted settings harness.
describe.skip('OpdsSettings', () => {
  it('needs a mounted settings harness for user CRUD and OPDS or Komga toggle branches', () => {
    expect.hasAssertions();
  });
});
