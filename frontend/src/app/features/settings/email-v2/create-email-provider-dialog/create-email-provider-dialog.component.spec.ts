import {describe, expect, it} from 'vitest';

// TODO(seam): Provider creation depends on dialog lifecycle, reactive forms, and email
// provider mutation services that should be covered through a mounted modal harness.
describe.skip('CreateEmailProviderDialogComponent', () => {
  it('needs a dialog harness for submit validation and provider creation error branches', () => {
    expect.hasAssertions();
  });
});
